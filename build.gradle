apply plugin: 'base'

configure (allprojects) { 
   
   
   ext {  
		springVersion = '4.1.4.RELEASE'
	}
}

subprojects {

    apply plugin: 'java'
    apply plugin: 'war'
    
    repositories {
        mavenCentral()
    }
}

